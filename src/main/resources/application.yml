server:
  port: 8080

spring:
  application:
    name: desafio-tecnico-amcom
  output:
    ansi:
      enabled: always
  main:
    lazy-initialization: false
  profiles:
    active: local

# ACTUATOR INFO - BEGIN
info:
  app:
    name: desafio-tecnico-amcom
    description: @project.description@
    version: @project.version@
    encoding: @project.build.sourceEncoding@
    java:
      version: @java.version@
# ACTUATOR INFO - END